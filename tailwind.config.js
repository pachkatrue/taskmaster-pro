/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  darkMode: 'class', // или 'media' для привязки к системным настройкам
  theme: {
    extend: {
      colors: {
        // Основной цвет и его оттенки
        primary: {
          lighter: '#8B5CF6', // Светлее для темной темы (фиолетовый)
          light: '#7C3AED',   // Светлый (для темной темы)
          DEFAULT: '#6D28D9', // Основной цвет (фиолетовый)
          dark: '#5B21B6',    // Темный оттенок (для светлой темы)
          darker: '#4C1D95',  // Более темный
        },
        // Цвета для фонов
        background: {
          light: '#F9FAFB', // Светлый фон (для светлой темы)
          dark: '#121827',  // Темный фон (для темной темы)
        },
        // Дополнительные цвета состояний
        success: {
          light: '#10B981', // Зеленый светлый
          dark: '#059669',  // Зеленый темный
        },
        warning: {
          light: '#F59E0B', // Желтый светлый
          dark: '#D97706',  // Желтый темный
        },
        danger: {
          light: '#EF4444', // Красный светлый
          dark: '#DC2626',  // Красный темный
        },
        info: {
          light: '#3B82F6', // Синий светлый
          dark: '#2563EB',  // Синий темный
        },
      },
      boxShadow: {
        card: '0 2px 8px rgba(0, 0, 0, 0.05)',
      },
      // Дополнительные цвета для серых оттенков в темной теме
      backgroundColor: {
        'gray-750': '#2D3748', // Промежуточный серый для темной темы
        'gray-850': '#1A202C', // Промежуточный тёмно-серый для темной темы
      },
      // Переходы
      transitionProperty: {
        'height': 'height',
        'spacing': 'margin, padding',
      },
    },
  },
  plugins: [],
}